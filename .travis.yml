sudo: required
services:
  - docker
env:
  global:
    - REGISTRY_USER=forcelinerobot
    - REGISTRY_PASS="789456qwe"

script:
  - docker build --tag forceline/myimage .
  - docker run forceline/myimage

after_script:
  - docker images

before_deploy:
  - docker login -u "$REGISTRY_USER" -p "$REGISTRY_PASS"
deploy:
  provider: script
  script: docker push forceline/myimage
  on:
    branch: test_docker
